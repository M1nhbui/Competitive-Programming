#include <bits/stdc++.h>
using namespace std;
#define MAX 100000
#define ll long long
#define lb long double

int main()
{
    ll n, m;
    cin >> n >> m;
    ll arr[MAX];
    for (ll i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    ll a, b, c;
    cin >> a >> b >> c;
    for (ll i = 0; i < n; i++)
    {
        
    }
    return 0;
}